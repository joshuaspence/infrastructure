---
# `ansible_facts.architecture` reports the kernel architecture (e.g. `aarch64`), whereas apt expects the Debian architecture (e.g. `arm64`).
github_cli_architectures:
  aarch64: 'arm64'
  armv7l: 'armhf'
  x86_64: 'amd64'
