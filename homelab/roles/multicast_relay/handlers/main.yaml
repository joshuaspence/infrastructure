---
- name: 'Reload systemctl daemon'
  become: true
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: 'Restart the multicast relay'
  become: true
  ansible.builtin.systemd_service:
    name: 'multicast-relay.service'
    state: 'restarted'
